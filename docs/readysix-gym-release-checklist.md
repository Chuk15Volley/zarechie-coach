# Условия закрытия задачи ReadySix → зал

## Фактический статус выпуска 16 сентября 2026

- PASS: ReadySix #416 опубликован; production workflow 35103814786 завершён успешно, commit f85cbecd60aaaa3e5cecc116f2f9b2b159409988.
- PASS: coach #38 опубликован; дополнительное исправление #39 устраняет блокировку расхождений, уже разрешённых основным календарём ReadySix. Production commit 1ff379bdf450bf74ca259dc22702113c8f9adeca, Vercel success.
- PASS: оба рабочих пространства отвечают 200 и передают новое состояние и календарь ReadySix (15 и 3 игрока).
- PASS: реальный день матча предлагает match_day; подтверждённый выходной остаётся no_gym.
- PASS: по выбору пользователя сохранение проверено только в изолированном контуре. Save → get сохраняют упражнения, дату и ReadySix revision; второе рабочее пространство не видит запись. Назначений игрокам не было.
- PASS: 382 теста coach, production build и повторный изолированный HTTP/browser smoke после исправлений.
- Проверка реальных черновиков разрешена пользователем; используются только два согласованных исторических контекста, autoSave=false.
- Реальные ответы выявили ложный подсчёт лёгкой профилактики как тяжёлой работы, неполный STOP силовых подходов и отсутствие попытки исправления дозировки в несиловых методах. Исправления добавлены с регрессионными тестами.
- IN PROGRESS: повторная проверка пяти методов после публикации этих исправлений. До её завершения приёмка не закрыта.

## Проверенная область

- ReadySix отвечает за состояние, ограничения и календарь; coach — за программу зала.
- Проверены стоп/выходной в синхронной и асинхронной генерации, новый стоп во время ожидания, применение предложения в браузере, сохранение источника/revision.
- Реальная история участвует в проверке повторов: две последние программы и две программы того же метода. Допускаются до двух главных якорей; защищённые протоколы имеют приоритет.
- MD−1 в реальном календаре выбранных дат — выходной, поэтому положительный тест метода активации выполняется на разрешённом тренировочном дне. Блокировка выходного и правила MD−1 покрыты отдельно.
- Сохранение проверяется только на синтетическом игроке в изолированном контуре, согласно выбору пользователя. Реальные тестовые назначения запрещены.

## Эксплуатация

Тренер поддерживает расписание и наблюдения в ReadySix, устраняет неразрешённые конфликты в Reports Center, проверяет предложенный вариант и итоговую программу перед назначением. Отсутствующие будущие игры не выдумываются генератором.

## Откат

При ошибке потребителя вернуть предыдущий deployment coach. Дополнительные поля upstream совместимы с прежним v1-потребителем, поэтому откат ReadySix обычно не нужен. Не переключать команду на legacy и не менять ключи автоматически. При недоступности аналитики приложение должно сообщить об этом и не подменять состояние старым расчётом.
